# TODO - 数据库异步化重构 (后续事宜)

目前数据库底层读取操作的异步化升级已平稳落地，但基于当前系统的复杂度与功能延伸性，为您梳理以下可以接着发力的系统建设重点：

1. **压测与性能验证：**
   - **建议操作**: 请利用相关的接口性能测试工具（如 `locust` 或者压测脚本），对 `/stream` 以及系统总览面板 `/stats` 发起真实的批量并发查询。
   - **理由**: 通过真实并发指标对比（如旧版 `sqlite3` 同步版和现版本的 QPS 和延迟数据），确保系统达到了我们预期的承载力。

2. **配置更细力度的连接池池化方案：**
   - **建议操作**: 当并发确实庞大时，`aiosqlite` 在不同线程池间虽然是非阻塞的，但由于 SQLite 本身的文件并发锁限制，可以考虑设置更高的超时重试阈值和连接复用。后续项目成熟后，可进一步将连接管理器改造为连接池获取形式。

3. **清理与废弃审查：**
   - **建议操作**: 后续可以扫描项目是否还存在 FastAPI 端点（非 CLI 管理或写入节点）内的长耗时同步磁盘 I/O。我们可以基于这次的改造样板对其进行进一步修缮。

如果您有任何特定的方向需要跟进解决（诸如配置性能调优或下一步重构），您可以直接指出，我将为您继续提供编码协助。
