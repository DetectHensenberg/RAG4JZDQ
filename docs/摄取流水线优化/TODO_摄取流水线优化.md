# TODO: 后续行动与验证操作指引

目前摄取流水线的深度改造与代码优化已全部交钥匙竣工。为了让改动跑出实际效果，你需要手动进行以下体验验证：

## 1. 重新洗库与触发全量摄取 (Reingest)
由于 PDF 解析底座的代码已彻底蜕变，你需要把之前的旧残数据刷掉，让系统用新的双核心引擎（极速分栏文字排版 + 整页高清截帧）来洗一遍文档。

**操作指引：**
- **通过 Web 界面**：在你之前的 Vue 前端 Dashboard，把之前的那些带有插图测试的 PDF/PPT 文档删除出库，然后重新点击上传摄取。
- **通过后台脚本**：重启你的 `start_vue.bat` / 8001 端口服务，确保它载入被我魔改后的最新 Python 代码，然后通过 API 或入库脚本重新投喂文档。

## 2. 验收视觉感知与 Markdown 美观度
入库成功后，去“知识库详情”看看被还原出来的 `Document` 预览文本：
**验收标准：**
1. 看看原本串行的多栏文本，现在是否非常干净利落地被组织成了正确的阅读顺序。
2. 看看那些带着插图或复杂表格的页码，在文本下方是不是优雅地出现了一行 `[IMAGE: xxx]` 标记。
3. 如果此时向大模型提问（RAG 提取到该段落），大模型就能把该位置截取出的大视野无损图片甩给前端了！

如果这几点验证通过，这项优化就算彻底、完美地落幕了！请随时在命令行或前端测试跑通后，抛回来跟我分享成效！
